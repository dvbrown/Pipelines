/* Teddy change the "distance" number from 1316 to the number you
measure by using the calibration tool. 
The known distance is the value that you measured ie 1mm. */

run("Set Scale...", "distance=1316 known=1000 pixel=1 unit=uM global");
